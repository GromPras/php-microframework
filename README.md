# PHP Micro-framework

## Docker

1. Start the app
```sh
docker-compose up --build

# Or start in detached mode
docker-compose up --build
```

1. Use composer
```sh
docker-compose exec app_name composer
```
